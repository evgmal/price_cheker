# Инструкция для администраторов

## Доступ к настройкам

Настройки приложения скрыты от обычных пользователей для предотвращения случайных изменений конфигурации.

### Как открыть настройки

1. **Быстро кликните 7 раз** по заголовку "📦 Прайс Чекер" в верхней части приложения
2. После разблокировки появится:
   - Уведомление "🔓 Настройки разблокированы"
   - Кнопка "⚙️ Настройки" станет видимой
3. Нажмите на кнопку "⚙️ Настройки" для изменения конфигурации

### Что можно настроить

- **URL для проверки цен** - адрес вашего API сервера
- **Логин** - логин для Basic HTTP Authentication (по умолчанию: "Администратор")
- **Пароль** - пароль для Basic HTTP Authentication (по умолчанию: пусто)

### Примеры настройки URL

```
# С параметром в URL
https://api.example.com/price?barcode={barcode}

# С плейсхолдером в пути
https://api.example.com/product/{barcode}

# Локальный сервер (для тестирования)
http://localhost:3000/price?barcode={barcode}
```

Токен `{barcode}` будет автоматически заменен на отсканированный штрих-код.

### Требования к API

Ваш API должен возвращать JSON с обязательным полем `price`:

```json
{
  "price": 199.99,
  "name": "Название товара (опционально)",
  "barcode": "1234567890123 (опционально)"
}
```

### Сброс настроек

Если нужно сбросить настройки к значениям по умолчанию:

1. Откройте консоль разработчика (F12)
2. Выполните команду:
   ```javascript
   localStorage.removeItem('apiUrl');
   localStorage.removeItem('apiUsername');
   localStorage.removeItem('apiPassword');
   location.reload();
   ```

### Безопасность

- Настройки сохраняются в LocalStorage браузера на устройстве
- При переустановке приложения настройки сохраняются
- Для полной очистки данных используйте "Очистить данные сайта" в настройках браузера

**Важно:**
- Логин и пароль хранятся в LocalStorage в открытом виде
- Не используйте это приложение на общедоступных устройствах без соблюдения мер безопасности
- Используется Basic HTTP Authentication с поддержкой кириллицы (UTF-8)

### Технические детали

#### Как работает разблокировка

- Требуется 7 кликов по заголовку в течение 2 секунд
- После разблокировки кнопка настроек остается видимой до перезагрузки страницы
- При перезагрузке страницы настройки снова скрываются

#### Изменение количества кликов

Чтобы изменить количество необходимых кликов:

1. Откройте файл `app.js`
2. Найдите строку:
   ```javascript
   if (this.clickCount >= 7 && !this.settingsUnlocked) {
   ```
3. Измените число `7` на желаемое количество кликов

#### Альтернативные методы доступа

Если нужно открыть настройки программно:

```javascript
// Откройте консоль разработчика (F12) и выполните:
document.getElementById('settings-toggle').classList.remove('hidden');
```

### Отладка

Для просмотра прогресса разблокировки:

1. Откройте консоль разработчика (F12)
2. При кликах по заголовку вы увидите сообщения:
   ```
   Осталось кликов: 4
   Осталось кликов: 3
   ...
   ```

### Примечания

- Эта функция предназначена для защиты от случайных изменений пользователями
- Это НЕ является серьезной защитой безопасности
- Опытный пользователь может найти способ открыть настройки через консоль разработчика
- Для реальной защиты настроек используйте серверную конфигурацию
