# Прайс Чекер - PWA приложение

Веб-приложение для проверки цен товаров по штрих-коду с возможностью установки как PWA на Android устройства.

## Возможности

- Сканирование штрих-кодов через USB/Bluetooth сканер
- HTTP запросы для получения цен товаров
- Анимация загрузки во время запроса
- История последних проверок (хранится локально)
- Настройка API endpoint
- Установка как PWA (Progressive Web App)
- Работа в офлайн режиме для интерфейса
- Адаптивный дизайн для мобильных устройств

## Требования

- Android устройство
- Браузер Google Chrome (рекомендуется версия 80+)
- Веб-сервер для размещения приложения (или локальный сервер для разработки)
- API сервер для получения цен товаров

## Установка

### 1. Размещение файлов

Разместите все файлы проекта на веб-сервере:
```
price_checker/
├── index.html
├── app.js
├── styles.css
├── manifest.json
├── sw.js
├── icon-192.png (создайте иконку)
└── icon-512.png (создайте иконку)
```

### 2. Создание иконок

Создайте иконки приложения:
- `icon-192.png` - 192x192 пикселей
- `icon-512.png` - 512x512 пикселей

Можно использовать любое изображение или создать с помощью онлайн генераторов иконок.

### 3. Настройка API

**Для администраторов:** Настройки скрыты от обычных пользователей.

Для доступа к настройкам:
1. Быстро кликните **7 раз** по заголовку "📦 Прайс Чекер"
2. Появится уведомление "🔓 Настройки разблокированы"
3. Нажмите кнопку "⚙️ Настройки"
4. Введите URL вашего API сервера
5. Нажмите "Сохранить"

Примеры форматов URL:
```
https://api.example.com/price?barcode={barcode}
https://api.example.com/product/{barcode}
```

Токены `{barcode}` будут автоматически заменены на отсканированный штрих-код.

**Подробнее:** См. [ADMIN.md](ADMIN.md) для детальной инструкции по настройке.

### 4. Установка PWA на Android

1. Откройте приложение в Chrome на Android устройстве
2. Откройте меню Chrome (три точки)
3. Выберите "Добавить на главный экран" или "Установить приложение"
4. Приложение появится на главном экране как обычное приложение

## Формат API

### Запрос

```
GET https://your-api.com/price?barcode=1234567890123
```

### Ответ

Ваш API должен возвращать JSON в следующем формате:

```json
{
  "barcode": "1234567890123",
  "name": "Название товара",
  "price": 199.99,
  "currency": "RUB"
}
```

Минимально необходимые поля:
- `price` (обязательно) - цена товара в числовом формате

Опциональные поля:
- `name` или `productName` - название товара
- `currency` - валюта (не используется в текущей версии)

### Пример Mock API сервера

Для тестирования можно использовать `mock-api.js`:

```bash
node mock-api.js
```

## Использование

### С USB/Bluetooth сканером

1. Подключите сканер к Android устройству
2. Откройте приложение
3. Отсканируйте штрих-код
4. Приложение автоматически отправит запрос и покажет цену

### Ручной ввод

1. Введите штрих-код вручную в поле ввода
2. Нажмите "Проверить цену"
3. Дождитесь результата

## Разработка

### Локальный сервер

Для локальной разработки можно использовать любой HTTP сервер:

```bash
# Python 3
python -m http.server 8000

# Node.js (http-server)
npx http-server -p 8000

# PHP
php -S localhost:8000
```

Затем откройте http://localhost:8000 в браузере.

### Структура проекта

- `index.html` - главная страница приложения
- `app.js` - основная логика приложения
- `styles.css` - стили и анимации
- `manifest.json` - манифест PWA
- `sw.js` - Service Worker для офлайн работы
- `mock-api.js` - тестовый API сервер

## Устранение неполадок

### PWA не устанавливается

- Убедитесь, что сайт работает через HTTPS (обязательно для PWA)
- Проверьте, что manifest.json доступен
- Проверьте консоль браузера на ошибки Service Worker

### Сканер не работает

- Убедитесь, что сканер работает в режиме "клавиатура"
- Проверьте, что поле ввода активно (курсор мигает)
- Некоторые сканеры требуют настройки для добавления Enter после сканирования

### Ошибки API

- Проверьте настройки API в приложении
- Убедитесь, что API доступен с вашего устройства
- Проверьте формат ответа API (должен быть JSON с полем price)
- Проверьте CORS настройки на сервере API

## Лицензия

MIT License - используйте свободно для коммерческих и личных проектов.

## Поддержка

Для вопросов и предложений создавайте Issue в репозитории проекта.
